{
  "name": "@email-notif/campaigns",
  "version": "0.1.0",
  "private": true,
  "description": "Dominio de campañas: programación (scheduling), envío automático y cancelación.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test",
    "lint": "node --check src/index.js && node --check src/campaignScheduler.js && node --check src/campaignRepository.js && node --check src/errors.js",
    "build": "node --check src/index.js"
  }
}
