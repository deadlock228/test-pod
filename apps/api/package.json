{
  "name": "@email-notif/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API backend: autenticación JWT y multi-tenant (slice auth-multitenant).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "lint": "node --check src/server.js"
  }
}
